---
const year = new Date().getFullYear()
const links = [
  { label: "Email", href: "mailto:hello@nbdevlab.com" },
  { label: "GitHub", href: "https://github.com/0xC0FFEEBEEF/nbdevlab" },
  { label: "Status", href: "/status/" },
]
---

<footer class="site-footer">
  <div class="site-footer__inner">
    <div class="site-footer__brand">
      <span class="site-footer__glyph" aria-hidden="true"></span>
      <div>
        <span class="site-footer__title">nbdevlab</span>
        <p>Experiments and tools crafted by Nathan Bullock.</p>
      </div>
    </div>
    <nav class="site-footer__nav" aria-label="Footer links">
      {links.map((link) => (
        <a
          href={link.href}
          target={link.href.startsWith("http") ? "_blank" : undefined}
          rel={link.href.startsWith("http") ? "noopener noreferrer" : undefined}
        >
          {link.label}
          <span aria-hidden="true">↗</span>
        </a>
      ))}
    </nav>
  </div>
  <p class="site-footer__meta">© {year} nbdevlab. Running on Cloudflare Pages.</p>
</footer>
<style>
  .site-footer {
    margin-top: 6rem;
    padding: 3rem 1.5rem 4rem;
    background: radial-gradient(circle at top, rgba(255, 145, 0, 0.12), rgba(10, 10, 10, 0.95) 55%), rgba(8, 8, 8, 0.92);
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 -24px 40px -32px rgba(0, 0, 0, 0.75);
  }
  .site-footer__inner {
    max-width: 1100px;
    margin: 0 auto 2rem;
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    align-items: center;
    justify-content: space-between;
  }
  .site-footer__brand {
    display: flex;
    align-items: center;
    gap: 1rem;
    color: rgba(243, 243, 243, 0.92);
  }
  .site-footer__glyph {
    width: 2.8rem;
    height: 2.8rem;
    border-radius: 1rem;
    background: linear-gradient(135deg, rgba(255, 145, 0, 0.85), rgba(255, 180, 80, 0.45));
    box-shadow: 0 12px 24px -16px rgba(255, 145, 0, 0.9);
    position: relative;
  }
  .site-footer__glyph::after {
    content: "";
    position: absolute;
    inset: 20%;
    border-radius: 0.8rem;
    background: rgba(10, 10, 10, 0.6);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.18);
  }
  .site-footer__title {
    font-size: 1.2rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    display: block;
  }
  .site-footer__brand p {
    margin: 0.1rem 0 0;
    color: rgba(220, 220, 220, 0.78);
    font-size: 0.95rem;
  }
  .site-footer__nav {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .site-footer__nav a {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.55rem 0.9rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: rgba(244, 244, 244, 0.9);
    text-decoration: none;
    background: rgba(17, 17, 17, 0.75);
    transition: border-color 0.2s ease, color 0.2s ease, transform 0.2s ease;
  }
  .site-footer__nav a span {
    transition: transform 0.2s ease;
  }
  .site-footer__nav a:hover,
  .site-footer__nav a:focus-visible {
    border-color: rgba(255, 145, 0, 0.6);
    color: rgba(255, 196, 140, 0.95);
    transform: translateY(-1px);
  }
  .site-footer__nav a:hover span,
  .site-footer__nav a:focus-visible span {
    transform: translate(2px, -2px);
  }
  .site-footer__meta {
    margin: 0;
    text-align: center;
    color: rgba(200, 200, 200, 0.7);
    font-size: 0.85rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  @media (max-width: 640px) {
    .site-footer {
      padding: 2.5rem 1.25rem 3.5rem;
    }
    .site-footer__inner {
      align-items: flex-start;
    }
  }
</style>
